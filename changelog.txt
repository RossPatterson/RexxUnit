REXXUNIT Change LOG
===================
1.1.2 - 2026-04-04
      - Make fixdoc reusable for other packages.
      - It should be an error to find no tests.
      - Make CMS documentation.
      - Add line number to $RXU_TrapSprung failure status.
      - Error in CMS not accepting filetype.

1.1.1 - 2026-02-08
      - Add packaging for CMS and the ASCII world.
      - Add CMS help file.
      - Add Expect('NORMAL')
      - Fix a couple of not-equals uses.

I0001 - Pattern matching doesn't work for finding tests.
      - Set exit return code to count of tests in FAIL, ERROR, SIGNAL and XFAIL.
      - Change TRACE (implied "I") option to TRACE <type>.
      - Patch around bREXX "EXPOSE (varlist)" bug in AssertStem...().

1.1.0 - 2025-11-29
      - Fix Fail() output.
      - More direct SystemInterface() path.
      - Add TRACE option.
      - Update examples.

1.0.0 - 2025-07-12
      - Make AssertStem...() show the item number.
      - Add a SYNCOUTPUT service, so CMS will flush console output at end of test.
      - All $RXU variables should be $RXU.whatever.
      - Explain Not()
      - Add AssertStemEqual() and AssertStemIdentical()
      - Make NoError() expose caller's vars, so EXECCOMM works for them.
      - First release.
